server:
  port: 8087

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/service_driver?characterEncoding=utf-8&serverTimeZone=GMT%2B8&useSSL=false
    username: root
    password: root

  application:
    name: service-driver-user